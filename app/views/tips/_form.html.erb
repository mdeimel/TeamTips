<%= form_for(@tip) do |f| %>
  <% if @tip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tip.errors.count, "error") %> prohibited this tip from being saved:</h2>

      <ul>
      <% @tip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= hidden_field_tag :updated_at, @tip.updated_at %>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title, :size => 78 %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body, :cols => 100 %>
  </div>
  <% if session[:user].nil? %>
    <div class="field">
      <%= f.label :user %><br />
      <%= f.text_field :user %>
    </div>
  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
